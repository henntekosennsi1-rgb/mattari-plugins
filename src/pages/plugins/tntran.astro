---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<Layout title="Tntran - MattariMinecraft">
  <Header currentPage="plugins" />

  <main class="plugin-detail">
    <div class="plugin-header">
      <div class="plugin-icon">💣</div>
      <div class="plugin-title-area">
        <h1>Tntran</h1>
        <p class="plugin-subtitle">TNT Runスタイルのミニゲームプラグイン</p>
        <div class="plugin-meta">
          <span class="badge version">v1.1</span>
          <span class="badge category">ミニゲーム</span>
          <span class="badge mc-version">1.21.x</span>
        </div>
      </div>
    </div>

    <div class="plugin-container">
      <aside class="sidebar">
        <div class="info-card">
          <h3>📋 プラグイン情報</h3>
          <table>
            <tr><td>バージョン</td><td>1.1</td></tr>
            <tr><td>対応MC</td><td>1.21.x</td></tr>
            <tr><td>作者</td><td>henry</td></tr>
            <tr><td>カテゴリ</td><td>ミニゲーム</td></tr>
          </table>
        </div>

        <div class="download-card">
          <h3>⬇️ ダウンロード</h3>
          <a href="https://www.spigotmc.org/resources/tntran-tnt-run-minigame-alpha.131733/" class="btn btn-spigot" target="_blank">SpigotMC</a>
          <a href="https://modrinth.com/plugin/tntran" class="btn btn-modrinth" target="_blank">Modrinth</a>
        </div>

        <div class="dependency-card">
          <h3>📦 依存関係</h3>
          <p class="no-dep">依存プラグインなし</p>
        </div>
      </aside>

      <article class="content">
        <section>
          <h2>📖 概要</h2>
          <p>Tntranは、足元のブロックが次々と消えていく「TNT Run」スタイルのミニゲームプラグインです。</p>
          <p>プレイヤーは走り続けなければ落下！最後まで生き残ったプレイヤーが勝利します。シンプルながら白熱するパーティーゲームです。</p>
        </section>

        <section>
          <h2>🎮 ゲームルール</h2>
          <div class="rule-box">
            <div class="rule-item">
              <span class="rule-icon">🏃</span>
              <div>
                <strong>走り続けろ！</strong>
                <p>足元のブロックに乗ると、一定時間後に消滅します</p>
              </div>
            </div>
            <div class="rule-item">
              <span class="rule-icon">⬇️</span>
              <div>
                <strong>落ちたら負け</strong>
                <p>ブロックがなくなって落下したらゲームオーバー</p>
              </div>
            </div>
            <div class="rule-item">
              <span class="rule-icon">🏆</span>
              <div>
                <strong>最後の一人が勝利</strong>
                <p>最後まで落ちずに残ったプレイヤーの勝ち！</p>
              </div>
            </div>
          </div>
        </section>

        <section>
          <h2>✨ 特徴</h2>
          <div class="feature-grid">
            <div class="feature-card">
              <span class="icon">⏱️</span>
              <h4>調整可能な消滅時間</h4>
              <p>ブロックが消えるまでの時間をTick単位で設定可能</p>
            </div>
            <div class="feature-card">
              <span class="icon">🚪</span>
              <h4>ロビーシステム</h4>
              <p>参加・退出がスムーズなロビー機能搭載</p>
            </div>
            <div class="feature-card">
              <span class="icon">👥</span>
              <h4>最大6人対戦</h4>
              <p>デフォルトで最大6人まで同時プレイ可能</p>
            </div>
            <div class="feature-card">
              <span class="icon">🔧</span>
              <h4>簡単セットアップ</h4>
              <p>setupコマンドで各地点を簡単に設定</p>
            </div>
          </div>
        </section>

        <section>
          <h2>⌨️ コマンド</h2>
          <div class="table-responsive">
            <table class="command-table">
              <thead>
                <tr><th>コマンド</th><th>説明</th><th>権限</th></tr>
              </thead>
              <tbody>
                <tr><td><code>/tntran join</code></td><td>ゲームに参加</td><td>tntran.use</td></tr>
                <tr><td><code>/tntran leave</code></td><td>ゲームから退出</td><td>tntran.use</td></tr>
                <tr><td><code>/tntran start</code></td><td>ゲームを開始</td><td>tntran.admin</td></tr>
                <tr><td><code>/tntran stop</code></td><td>ゲームを強制終了</td><td>tntran.admin</td></tr>
                <tr><td><code>/tntran setup</code></td><td>セットアップモード</td><td>tntran.admin</td></tr>
                <tr><td><code>/tntran reload</code></td><td>設定をリロード</td><td>tntran.admin</td></tr>
              </tbody>
            </table>
          </div>
        </section>

        <section>
          <h2>🔑 権限</h2>
          <div class="table-responsive">
            <table class="command-table">
              <thead>
                <tr><th>権限ノード</th><th>説明</th><th>デフォルト</th></tr>
              </thead>
              <tbody>
                <tr><td><code>tntran.use</code></td><td>基本コマンド（join, leave）</td><td>全員</td></tr>
                <tr><td><code>tntran.admin</code></td><td>管理者コマンド（start, stop, setup, reload）</td><td>OP</td></tr>
              </tbody>
            </table>
          </div>
        </section>

        <section>
          <h2>⚙️ 設定項目</h2>
          <div class="config-list">
            <div class="config-item">
              <code>decay-delay-ticks</code>
              <span>ブロック消滅までの時間（デフォルト: 20 = 1秒）</span>
            </div>
            <div class="config-item">
              <code>max-players</code>
              <span>最大参加人数（デフォルト: 6人）</span>
            </div>
            <div class="config-item">
              <code>locations.spawn</code>
              <span>初期リスポーン場所（ゲート前、途中抜け時の移動先）</span>
            </div>
            <div class="config-item">
              <code>locations.lobby</code>
              <span>ゲームロビー（受付エリア、看板設置場所）</span>
            </div>
            <div class="config-item">
              <code>locations.area.pos1/pos2</code>
              <span>ゲームエリア範囲（マップ生成＆TP先範囲）</span>
            </div>
          </div>

          <div class="notice notice-info" style="margin-top: 20px;">
            <strong>💡 Tick について</strong>
            <p>20 ticks = 1秒です。数値を小さくするとブロックが速く消え、難易度が上がります。</p>
          </div>
        </section>

        <section>
          <h2>🛠️ セットアップ手順</h2>
          <div class="steps">
            <div class="step">
              <div class="step-num">1</div>
              <div class="step-content">
                <strong>マップを用意</strong>
                <p>TNT Run用のフィールドを建築します。複数層のフロアがおすすめ！</p>
              </div>
            </div>
            <div class="step">
              <div class="step-num">2</div>
              <div class="step-content">
                <strong>セットアップ開始</strong>
                <p><code>/tntran setup</code>を実行してセットアップモードに入ります。</p>
              </div>
            </div>
            <div class="step">
              <div class="step-num">3</div>
              <div class="step-content">
                <strong>各地点を設定</strong>
                <p>以下の地点を順番に設定します：</p>
                <ul class="setup-list">
                  <li><strong>spawn</strong> - ゲーム終了後のテレポート先</li>
                  <li><strong>lobby</strong> - 待機ロビーの位置</li>
                  <li><strong>area pos1/pos2</strong> - ゲームエリアの対角2点</li>
                </ul>
              </div>
            </div>
            <div class="step">
              <div class="step-num">4</div>
              <div class="step-content">
                <strong>設定完了！</strong>
                <p><code>/tntran join</code>で参加してテストプレイ！</p>
              </div>
            </div>
          </div>
        </section>

        <section>
          <h2>🎮 遊び方</h2>
          <div class="play-flow">
            <div class="flow-item">
              <div class="flow-num">1</div>
              <div class="flow-text">
                <strong>参加</strong>
                <p><code>/tntran join</code>でロビーに参加</p>
              </div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-item">
              <div class="flow-num">2</div>
              <div class="flow-text">
                <strong>開始</strong>
                <p>管理者が<code>/tntran start</code>でゲーム開始</p>
              </div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-item">
              <div class="flow-num">3</div>
              <div class="flow-text">
                <strong>走れ！</strong>
                <p>足元が消える前に移動し続けろ！</p>
              </div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-item">
              <div class="flow-num">4</div>
              <div class="flow-text">
                <strong>勝利</strong>
                <p>最後まで残れば勝ち！</p>
              </div>
            </div>
          </div>
        </section>

        <section>
          <h2>📥 インストール</h2>
          <ol class="install-steps">
            <li>JARファイルをダウンロード</li>
            <li>サーバーの<code>plugins</code>フォルダに配置</li>
            <li>サーバーを再起動</li>
            <li>TNT Runマップを建築</li>
            <li><code>/tntran setup</code>で各地点を設定</li>
            <li>遊ぶ準備完了！</li>
          </ol>
        </section>
      </article>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  .plugin-detail { max-width: 1200px; margin: 0 auto; padding: 0 20px 60px; }
  .plugin-header { display: flex; align-items: center; gap: 25px; padding: 40px 0; color: white; }
  .plugin-icon { font-size: 64px; background: rgba(255,255,255,0.15); width: 100px; height: 100px; display: flex; align-items: center; justify-content: center; border-radius: 20px; }
  .plugin-title-area h1 { font-size: 36px; margin-bottom: 8px; }
  .plugin-subtitle { font-size: 18px; opacity: 0.9; margin-bottom: 15px; }
  .plugin-meta { display: flex; gap: 10px; flex-wrap: wrap; }
  .badge { padding: 6px 14px; border-radius: 20px; font-size: 13px; font-weight: 600; }
  .badge.version { background: #28a745; }
  .badge.category { background: #ff6b6b; }
  .badge.mc-version { background: #667eea; }

  .plugin-container { display: grid; grid-template-columns: 280px 1fr; gap: 30px; }
  .sidebar { display: flex; flex-direction: column; gap: 20px; }
  .info-card, .download-card, .dependency-card { background: white; border-radius: 15px; padding: 20px; }
  .info-card h3, .download-card h3, .dependency-card h3 { font-size: 14px; color: #333; margin-bottom: 15px; }
  .info-card table { width: 100%; font-size: 13px; }
  .info-card td { padding: 8px 0; border-bottom: 1px solid #eee; }
  .info-card td:first-child { color: #666; }
  .info-card td:last-child { font-weight: 600; text-align: right; }

  .btn { display: block; padding: 12px; text-align: center; border-radius: 8px; text-decoration: none; font-weight: 600; margin-bottom: 10px; transition: all 0.3s; }
  .btn:last-child { margin-bottom: 0; }
  .btn-spigot { background: #ee8a19; color: white; }
  .btn-spigot:hover { background: #d87a10; }
  .btn-modrinth { background: #1bd96a; color: white; }
  .btn-modrinth:hover { background: #17c45e; }
  .no-dep { font-size: 13px; color: #888; }

  .content { background: white; border-radius: 15px; padding: 40px; }
  .content h2 { font-size: 20px; color: #333; margin-bottom: 20px; padding-bottom: 10px; border-bottom: 2px solid #eee; }
  .content p { font-size: 15px; line-height: 1.8; color: #555; margin-bottom: 15px; }
  .content section { margin-bottom: 40px; }

  .rule-box { display: flex; flex-direction: column; gap: 15px; }
  .rule-item { display: flex; align-items: center; gap: 15px; padding: 20px; background: linear-gradient(135deg, #ff6b6b10, #ee5a5a10); border-radius: 12px; border-left: 4px solid #ff6b6b; }
  .rule-icon { font-size: 32px; }
  .rule-item strong { display: block; font-size: 16px; color: #333; margin-bottom: 5px; }
  .rule-item p { margin: 0; font-size: 14px; color: #666; }

  .feature-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; }
  .feature-card { background: #f8f9fa; border-radius: 12px; padding: 20px; }
  .feature-card .icon { font-size: 28px; display: block; margin-bottom: 10px; }
  .feature-card h4 { font-size: 15px; color: #333; margin-bottom: 8px; }
  .feature-card p { font-size: 13px; color: #666; margin: 0; }

  .table-responsive { overflow-x: auto; }
  .command-table { width: 100%; border-collapse: collapse; font-size: 14px; }
  .command-table th { background: #f8f9fa; padding: 12px; text-align: left; font-weight: 600; }
  .command-table td { padding: 12px; border-bottom: 1px solid #eee; }
  .command-table code { background: #e9ecef; padding: 3px 8px; border-radius: 4px; font-size: 13px; }

  .config-list { display: flex; flex-direction: column; gap: 10px; }
  .config-item { display: flex; align-items: center; gap: 15px; padding: 12px 15px; background: #f8f9fa; border-radius: 8px; }
  .config-item code { background: #667eea; color: white; padding: 4px 10px; border-radius: 4px; font-size: 12px; white-space: nowrap; }
  .config-item span { font-size: 14px; color: #555; }

  .notice { padding: 15px 20px; border-radius: 10px; }
  .notice strong { display: block; margin-bottom: 5px; }
  .notice p { margin: 0; font-size: 14px; }
  .notice-info { background: #e7f5ff; border-left: 4px solid #339af0; }

  .steps { display: flex; flex-direction: column; gap: 15px; }
  .step { display: flex; gap: 15px; padding: 15px; background: #f8f9fa; border-radius: 10px; }
  .step-num { width: 35px; height: 35px; background: linear-gradient(135deg, #667eea, #764ba2); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; flex-shrink: 0; }
  .step-content strong { display: block; margin-bottom: 5px; color: #333; }
  .step-content p { margin: 0 0 8px 0; font-size: 14px; color: #666; }
  .step-content code { background: #e9ecef; padding: 2px 6px; border-radius: 4px; font-size: 12px; }
  .setup-list { margin: 10px 0 0 20px; padding: 0; }
  .setup-list li { font-size: 13px; color: #555; margin-bottom: 5px; }

  .play-flow { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 10px; padding: 20px; background: #f8f9fa; border-radius: 12px; }
  .flow-item { display: flex; align-items: center; gap: 10px; }
  .flow-num { width: 30px; height: 30px; background: linear-gradient(135deg, #ff6b6b, #ee5a5a); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 14px; }
  .flow-text strong { display: block; font-size: 14px; color: #333; }
  .flow-text p { margin: 0; font-size: 12px; color: #666; }
  .flow-text code { background: #e9ecef; padding: 1px 4px; border-radius: 3px; font-size: 11px; }
  .flow-arrow { color: #ccc; font-size: 20px; font-weight: bold; }

  .install-steps { padding-left: 20px; }
  .install-steps li { margin-bottom: 10px; font-size: 14px; color: #555; }
  .install-steps code { background: #e9ecef; padding: 2px 6px; border-radius: 4px; }

  @media (max-width: 900px) {
    .plugin-container { grid-template-columns: 1fr; }
    .feature-grid { grid-template-columns: 1fr; }
    .play-flow { flex-direction: column; align-items: flex-start; }
    .flow-arrow { transform: rotate(90deg); align-self: center; }
  }
</style>
